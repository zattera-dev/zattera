# Binaries (repo-root build outputs only; anchored so they don't match
# api/proto/zattera, api/gen/zattera, or other nested paths)
/bin/
/dist/
/zattera
/zatterad
*.exe

# Test artifacts
coverage.out
coverage.html
*.test

# Data dirs from local runs
/data/
*.db

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# Env / local overrides
.env
.env.local

node_modules/

PERSONAL.md
TEST_SERVER.md

# Real-cluster e2e scripts: credentials + per-cluster state
/zt
test/real-cluster/servers.env
test/real-cluster/.state/
# docmd build output
/site/

# wrangler
.wrangler/
